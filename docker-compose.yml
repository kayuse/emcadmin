version: '3.8'

services:
  admin-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-https://api.ecwamediacenter.com}
    ports:
      - "5433:80"
    restart: always
